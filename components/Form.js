import { useState } from "react"

// A generic form to set values for fields

/**
 * props - an object with field values
*/
export default function Form({props}){

    const [formData, setFormData] = useState(null);


    return(
        <>
        form
        </>
    )



}